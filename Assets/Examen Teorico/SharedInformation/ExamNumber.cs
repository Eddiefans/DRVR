using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExamNumber : MonoBehaviour
{


    public static int examNumber;
    public static bool directFeedback = false;
    public static List<Falta> faltasAux = new List<Falta>();
    public static List<Criterio> criteriosAux = new List<Criterio>();
    public static int leccionNumber;
    public static bool feedbackP = false;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
